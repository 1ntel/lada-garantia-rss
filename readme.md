# RSS feed for lada-garantia
Rename `.\data\config.php.example` to `.\data\config.php` and write your credentials.
To collect data run `php .\update.php`.
File `index.php` is the actual RSS feed for the webserver.